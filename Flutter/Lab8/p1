import 'package:flutter/material.dart';

class p1 extends StatefulWidget {
  const p1({Key? key}) : super(key: key);

  @override
  _p1State createState() => _p1State();
}

class _p1State extends State<p1> {
  Widget button(String txt, [bool isGrey = false]) {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Container(
        child: Center(child: Text(txt, style: TextStyle(fontSize: 35))),
        height: 55,
        width: 55,
        decoration: BoxDecoration(
          color: const Color.fromARGB(255, 190, 190, 190),
          borderRadius: BorderRadius.circular(50),
        ),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Lab-8 Program-1"),
        centerTitle: true,
        backgroundColor: Colors.blue,
      ),
      body: Center(
        child: Container(
          height: 595,
          width: 350,
          decoration: BoxDecoration(
            color: const Color.fromARGB(255, 234, 234, 234),
            borderRadius: BorderRadius.circular(10),
          ),
          child: Column(
            children: [
              Container(
                height: 150,
                decoration: BoxDecoration(
                  borderRadius: BorderRadius.circular(10),
                ),
                child: Padding(
                  padding: const EdgeInsets.all(8.0),
                  child: Align(
                    alignment: Alignment.bottomRight,
                    child: Text(
                      "0",
                      style: TextStyle(
                        fontWeight: FontWeight.bold,
                        fontSize: 45,
                      ),
                    ),
                  ),
                ),
              ),
              Row(
                children: [
                  button("C", true),
                  button("{ }", true),
                  button("%", true),
                  button("/", true),
                ],
              ),
              Row(
                children: [
                  button("7"),
                  button("8"),
                  button("9"),
                  button("x", true),
                ],
              ),
              Row(
                children: [
                  button("4"),
                  button("5"),
                  button("6"),
                  button("-", true),
                ],
              ),
              Row(
                children: [
                  button("1"),
                  button("2"),
                  button("3"),
                  button("+", true),
                ],
              ),
              Row(
                children: [
                  button("."),
                  button("0"),
                  button("<-"),
                  button("=", true),
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }
}
